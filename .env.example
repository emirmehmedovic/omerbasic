# Prisma - Povezivanje s bazom podataka
# connection_limit=3 - limitira broj konekcija (preporučeno za VPS da smanji RAM usage)
# pool_timeout=20 - timeout za dobijanje konekcije iz pool-a (sekunde)
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require&connection_limit=3&pool_timeout=20"

# NextAuth.js - Tajni ključ za enkripciju sesija
# Generirajte ga s: openssl rand -base64 32
NEXTAUTH_SECRET=""

# NextAuth.js - URL aplikacije (potrebno za NextAuth)
# Za development: http://localhost:3000
# Za produkciju: https://vasadomena.com
NEXTAUTH_URL="http://localhost:3000"

# Resend - API ključ za slanje emailova
RESEND_API_KEY=""

# Opcionalno: Konfiguracija za vlastiti SMTP server (ako ne koristite Resend)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM=""

# Javna URL adresa aplikacije (koristi se za generiranje linkova u emailovima)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

USE_TRIGRAM_LISTING_SEARCH="false"
USE_TRIGRAM_ADVANCED_SEARCH="false"
